# US-37 — i18n setup + language switcher — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up Django i18n end-to-end — settings + `LocaleMiddleware` + `set_language` + a navbar language switcher — and prove it by translating the navbar (the demonstrable slice) into English.

**Architecture:** Settings gain `LANGUAGES`/`LOCALE_PATHS`/`LocaleMiddleware`; `settings/urls.py` mounts `i18n/`; `base.html` loads `i18n`, sets `<html lang>`, wraps the navbar strings in `{% trans %}`, and adds a select-auto-submit switcher. `locale/en` translates the navbar slice; `.mo` is committed. Exhaustive translation is US-38.

**Tech Stack:** Django i18n (`LocaleMiddleware`, `{% trans %}`, `set_language`, gettext); pytest / pytest-django.

---

## Role division

- **User** writes app files (`settings/*.py`, `templates/base.html`, `locale/**`) and runs all `git`/`pytest`/`manage.py` commands.
- **Claude** writes all test code (`tests/**`).

## Spec

`docs/superpowers/specs/2026-05-25-us37-i18n-setup-design.md`

## File structure

| File | Action | Responsibility | Author |
|------|--------|----------------|--------|
| `settings/base.py` | modify | `LANGUAGES`, `LOCALE_PATHS`, `LocaleMiddleware`, `gettext_lazy` import | User |
| `settings/urls.py` | modify | mount `i18n/` | User |
| `templates/base.html` | modify | `{% load i18n %}`, `<html lang>`, navbar `{% trans %}`, switcher | User |
| `locale/en/LC_MESSAGES/django.{po,mo}` | create | English navbar translations (committed) | User |
| `locale/pl/LC_MESSAGES/django.{po,mo}` | create | Polish catalog (empty msgstrs, committed) | User |
| `tests/test_i18n.py` | create | LANGUAGES, default PL, switcher, switch-to-EN | Claude |

No migration.

## TDD note

The i18n feature is config + templates + a compiled catalog (not a function), so the tests are integration checks: `test_languages_setting` and the switcher/switch tests go red first (exact `LANGUAGES`, `NoReverseMatch` on `set_language`), then green once Task 2's infra + compiled `en` `.mo` land. `test_default_language_polish` is a regression guard (the navbar already shows "Repertuar"). Commit on green; the first commit folds in `.Claude/m5_planning.md`.

---

### Task 1: Write the i18n tests (red)

**Files:**
- Create: `tests/test_i18n.py`

- [ ] **Step 1 [Claude]: Write the tests**

Create `tests/test_i18n.py`:
```python
import pytest
from django.conf import settings
from django.urls import reverse

pytestmark = pytest.mark.django_db


def test_languages_setting():
    codes = {code for code, _label in settings.LANGUAGES}
    assert codes == {"pl", "en"}  # our restricted set, not Django's full default list


def test_default_language_polish(client):
    resp = client.get("/")
    assert resp.status_code == 200
    assert b"Repertuar" in resp.content  # pl falls back to the Polish msgid


def test_switcher_rendered(client):
    resp = client.get("/")
    content = resp.content.decode()
    assert reverse("set_language") in content  # /i18n/setlang/
    assert 'value="pl"' in content
    assert 'value="en"' in content


def test_switch_to_english(client):
    resp = client.post(reverse("set_language"), {"language": "en", "next": "/"})
    assert resp.status_code == 302
    home = client.get("/")
    # Navbar "Repertuar" -> "Now Showing" (needs the compiled en .mo). We assert the
    # translated string is present rather than "Repertuar" absent — the page body has other
    # not-yet-translated Polish (that's US-38), so an absence check would be brittle.
    assert b"Now Showing" in home.content
```

- [ ] **Step 2 [User]: Run to confirm FAIL**

Run: `poetry run pytest tests/test_i18n.py -q --no-cov`
Expected: FAIL — `test_languages_setting` (Django's default LANGUAGES isn't `{pl, en}`),
`test_switcher_rendered`/`test_switch_to_english` (`NoReverseMatch: 'set_language'` — the
`i18n/` URL isn't mounted yet). `test_default_language_polish` likely already passes.

---

### Task 2: i18n infrastructure + navbar slice

**Files:**
- Modify: `settings/base.py`, `settings/urls.py`, `templates/base.html`
- Create: `locale/{en,pl}/LC_MESSAGES/django.{po,mo}`

- [ ] **Step 1 [User]: Verify gettext tools (the #1 M5 risk)**

Run: `which xgettext msgfmt`
Expected: both resolve. **If not, install gettext tools first** (`choco install gettext`,
MSYS2 `pacman -S gettext`, or the mlocati gettext-iconv-windows build) and re-check — Django's
`makemessages` (`xgettext`) and `compilemessages` (`msgfmt`) need them.

- [ ] **Step 2 [User]: Settings — `settings/base.py`**

Add at the top with the other imports:
```python
from django.utils.translation import gettext_lazy as _
```
Add (near the localization block):
```python
LANGUAGES = [("pl", _("Polski")), ("en", _("English"))]
LOCALE_PATHS = [BASE_DIR / "locale"]
```
In `MIDDLEWARE`, insert `LocaleMiddleware` **after** `SessionMiddleware`, **before**
`CommonMiddleware`:
```python
MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.locale.LocaleMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
]
```

- [ ] **Step 3 [User]: URL — `settings/urls.py`**

Add to `urlpatterns` (e.g. after the `admin/` line):
```python
    path("i18n/", include("django.conf.urls.i18n")),
```
(`include` is already imported.)

- [ ] **Step 4 [User]: Replace `templates/base.html`**

Full file (note: every `{% trans %}` stays on one line — PyCharm hard-wrap breaks the tag,
dev pitfall #6):
```html
{% load static i18n %}
{% get_current_language as LANGUAGE_CODE %}
<!DOCTYPE html>
<html lang="{{ LANGUAGE_CODE }}" data-bs-theme="dark">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}KinoMania{% endblock %}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap">
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{% static 'css/theme.css' %}">
    <link rel="stylesheet" href="{% static 'css/components.css' %}">
</head>
<body class="d-flex flex-column min-vh-100">

<nav class="navbar navbar-expand-lg sticky-top"
     style="background: var(--bg-elev); border-bottom: 1px solid var(--border);">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">🎬 KinoMania</a>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#navMain"
                aria-controls="navMain" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMain">
            {% with un=request.resolver_match.url_name %}
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    {% if un == 'home' or un == 'movie_list' or un == 'movie_detail' %}
                    <a class="nav-link active" href="{% url 'cinema:home' %}">{% trans "Repertuar" %}</a>
                    {% else %}
                    <a class="nav-link" href="{% url 'cinema:home' %}">{% trans "Repertuar" %}</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if un == 'screening_list' %}
                    <a class="nav-link active" href="{% url 'cinema:screening_list' %}">{% trans "Seanse" %}</a>
                    {% else %}
                    <a class="nav-link" href="{% url 'cinema:screening_list' %}">{% trans "Seanse" %}</a>
                    {% endif %}
                </li>
                {% if user.is_authenticated %}
                <li class="nav-item">
                    {% if un == 'my_bookings' %}
                    <a class="nav-link active" href="{% url 'booking:my_bookings' %}">{% trans "Moje rezerwacje" %}</a>
                    {% else %}
                    <a class="nav-link" href="{% url 'booking:my_bookings' %}">{% trans "Moje rezerwacje" %}</a>
                    {% endif %}
                </li>
                {% endif %}
            </ul>
            {% endwith %}
            <ul class="navbar-nav align-items-lg-center">
                {% if user.is_authenticated %}
                <li class="nav-item me-lg-3">
                    <span class="navbar-text text-muted small">{{ user.email }}</span>
                </li>
                <li class="nav-item">
                    <form method="post" action="{% url 'accounts:logout' %}" class="d-inline">
                        {% csrf_token %}
                        <button type="submit" class="btn btn-outline-light btn-sm">{% trans "Wyloguj" %}</button>
                    </form>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'accounts:login' %}">{% trans "Zaloguj" %}</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-primary btn-sm ms-lg-2" href="{% url 'accounts:register' %}">{% trans "Zarejestruj" %}</a>
                </li>
                {% endif %}
                <li class="nav-item ms-lg-2">
                    <form method="post" action="{% url 'set_language' %}" class="d-inline">
                        {% csrf_token %}
                        <input type="hidden" name="next" value="{{ request.get_full_path }}">
                        <select name="language" onchange="this.form.submit()" class="form-select form-select-sm" aria-label="{% trans 'Język' %}">
                            <option value="pl" {% if LANGUAGE_CODE == 'pl' %}selected{% endif %}>PL</option>
                            <option value="en" {% if LANGUAGE_CODE == 'en' %}selected{% endif %}>EN</option>
                        </select>
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>

<main class="container py-4 flex-grow-1">
    {% if messages %}
    {% for message in messages %}
    <div class="alert alert-{{ message.tags|default:'info' }} alert-dismissible fade show"
         role="alert">
        {{ message }}
        <button type="button" class="btn-close"
                data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {% endfor %}
    {% endif %}
    {% block content %}{% endblock %}
</main>

<footer class="py-3 mt-auto"
        style="background: var(--bg-elev); border-top: 1px solid var(--border);">
    <div class="container d-flex justify-content-between align-items-center small text-muted">
        <span>🎬 © 2026 KinoMania · projekt edukacyjny</span>
        <span>Powered by Django</span>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

- [ ] **Step 5 [User]: Generate message catalogs**

Run: `poetry run python manage.py makemessages -l en -l pl --ignore=.venv`
Expected: creates `locale/en/LC_MESSAGES/django.po` and `locale/pl/LC_MESSAGES/django.po`
with the navbar msgids (Repertuar, Seanse, Moje rezerwacje, Wyloguj, Zaloguj, Zarejestruj,
Polski, English, Język).

- [ ] **Step 6 [User]: Translate the English catalog**

In `locale/en/LC_MESSAGES/django.po`, fill the `msgstr`s (leave `locale/pl/...` empty —
Polish source falls back to the msgid):
```
msgid "Repertuar"        -> msgstr "Now Showing"
msgid "Seanse"           -> msgstr "Screenings"
msgid "Moje rezerwacje"  -> msgstr "My Bookings"
msgid "Wyloguj"          -> msgstr "Log out"
msgid "Zaloguj"          -> msgstr "Log in"
msgid "Zarejestruj"      -> msgstr "Sign up"
msgid "Polski"           -> msgstr "Polish"
msgid "English"          -> msgstr "English"
msgid "Język"            -> msgstr "Language"
```

- [ ] **Step 7 [User]: Compile**

Run: `poetry run python manage.py compilemessages`
Expected: writes `locale/en/LC_MESSAGES/django.mo` + `locale/pl/LC_MESSAGES/django.mo`.

- [ ] **Step 8 [User]: Run the i18n tests — green**

Run: `poetry run pytest tests/test_i18n.py -q --no-cov`
Expected: PASS (4 passed).

- [ ] **Step 9 [User]: Commit (folds in `m5_planning.md`)**

```bash
git add settings/base.py settings/urls.py templates/base.html locale/ tests/test_i18n.py .Claude/m5_planning.md
git commit -m "feat(FR-15): i18n setup + navbar language switcher (US-37)"
```

---

### Task 3: Quality gate

- [ ] **Step 1 [User]: Full suite with coverage**

Run: `poetry run pytest`
Expected: PASS, coverage ≥ 80%. (Watch the existing auth-template regression test
`tests/cinema/test_accounts_templates_regression.py` — it checks `<nav` + `🎬 KinoMania`
render; the navbar still has both, so it stays green.)

- [ ] **Step 2 [User]: Lint + format + type-check**

Run: `poetry run ruff check . && poetry run ruff format --check . && poetry run mypy .`
Expected: clean. (`gettext_lazy` import is used by `LANGUAGES`; `locale/` is ruff-excluded.)

- [ ] **Step 3 [User]: Manual smoke (optional)**

`runserver` → the navbar shows a PL/EN select; pick EN → page reloads, navbar reads
"Now Showing / Screenings / …"; pick PL → back to Polish. Choice persists across pages.

---

## Out of scope

Exhaustive string marking/translation (US-38) · error pages (US-39) · `i18n_patterns` on URLs.

## Test plan summary

- `tests/test_i18n.py`: `LANGUAGES == {pl, en}`; default `/` shows "Repertuar"; navbar renders
  the `set_language` switcher (PL/EN); `set_language` → EN flips "Repertuar" → "Now Showing".
- Existing suite unaffected (navbar regression test still sees `<nav` + brand).
- Coverage ≥ 80%; no migration.

## Note

`ruff` excludes `locale/` already (`extend-exclude` in `pyproject.toml`), so the `.po`/`.mo`
files won't trip lint. The committed `.mo` is what makes the English-switch test pass in CI.
