# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
.venv/
venv/
env/
ENV/

# Poetry
# (poetry.lock IS committed; only the cache is ignored)
.poetry-cache/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
# Compiled gettext catalogs (.mo) are committed so CI runs tests without a
# compilemessages step — re-run `manage.py compilemessages` after editing .po (US-37).

# Environment
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Coverage / Test
htmlcov/
.coverage
.coverage.*
.cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage.xml
*.cover

# Docker
docker-compose.override.yml

# Stripe CLI binary (downloaded per-machine, OS-specific — not tracked)
stripe.exe
stripe

# User uploads (Actor/Director photos, Movie posters) — exclude content, keep .gitkeep
media/*
!media/.gitkeep

.superpowers/
